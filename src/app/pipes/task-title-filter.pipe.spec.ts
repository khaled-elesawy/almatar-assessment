import { TaskTitleFilterPipe } from './task-title-filter.pipe';

describe('TaskTitleFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new TaskTitleFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
