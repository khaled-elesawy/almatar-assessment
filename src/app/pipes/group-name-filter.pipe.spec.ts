import { GroupNameFilterPipe } from './group-name-filter.pipe';

describe('GroupNameFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new GroupNameFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
